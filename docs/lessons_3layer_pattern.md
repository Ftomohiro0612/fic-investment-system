# 教訓即時反映の3層型（標準パターン）

**位置づけ**：段階4標準プロトコル「**定義→本作業→点検→教訓即時反映**」サイクルの**教訓即時反映の標準型**。Phase 5 移行の核心資産として、新subagent作成時・既存subagent改修時・本作業実走時に発見した教訓を「**ルール参照・教訓追跡・実装ゲート**」の3層に分離して反映することで、再現性（次回も同パターンで再発防止）を担保する。
**初版**＝2026-05-24（3861_oji_v4_pilot 縦ライン検証で L-021/L-022/L-023 の3事例が3層型で定着しFICが標準型として評価）。今後の事例で追補する。

---

## 1. 3層型の定義

| 層 | ファイル例 | 役割 |
|---|---|---|
| **層1：具体ルール** | 領域別ガイド（`docs/data_figure_lessons.md` / `docs/ai_image_lessons.md` / `docs/non_ai_structure_chart_lessons.md` 等） | 「**次に同じ作業をする時に何を守るか**」を具体ルールとして書く。後続の subagent / 人 が同じ領域の作業時に**ルール参照**する。 |
| **層2：教訓本体** | `docs/chat_workflows/_analysis/review_lessons_log.md` | 「**いつ・どこで・どう発見されたか・普遍性は**」を教訓として時系列で記録。L-NNN番号付き。後続の curation・蒸留・新subagent設計時に**教訓追跡**する。 |
| **層3：実装ゲート** | subagent定義（`.claude/agents/{role}.md`）／Skill（`.claude/skills/{skill}/SKILL.md`）／テンプレ（`wordpress/templates/*.html` 等） | 「**次回 subagent が起動した時に確実に実行する**」ゲートとして組み込む。subagentの挙動を実際に変える**実装ゲート**として機能。 |

### 3層分離の利点
- 段階3パイロット時代は教訓を1箇所に書いて終わりがちだったが、3層分離により「**ルール参照（層1）・教訓追跡（層2）・実装ゲート（層3）**」の役割が明確化。
- 新規 subagent作成時は層1+2でガイドを参照、改修時は層3で実装漏れを防ぐ、レビュー時は層2で教訓履歴を辿る、と各役割が独立して機能。
- §7ゲート対象は層3（`.claude/skills/` および `.claude/agents/` 編集はFIC承認）。層1・層2は §7対象外＝即時反映可。
- 「データ的一般化」（複数事例で初めて普遍性が見える教訓）はまず層2に記録、複数事例蓄積後に層1へ昇格、最後に層3に実装ゲート化、という**段階的昇格パス**が可能。

---

## 2. 3事例（参照型・2026-05-24時点）

### 事例1：L-021 SVGデータ図の見やすさ規律（3層全部）
- **発見**：3861パイロット FIC実機確認で SVG#1〜#3 全てに下部レイアウト見切れ・テキスト重なり・略語使用・符号表記の曖昧さが残存。
- **層1**：`docs/data_figure_lessons.md` に4セクション追補（注釈枠幅・viewBox高さ・テキスト重なり防止／略語禁止／感応度バー符号の一貫性／データ図ブラウザ自己検証の必須化）。
- **層2**：`docs/chat_workflows/_analysis/review_lessons_log.md` L-021 エントリ。
- **層3**：`.claude/agents/designer.md` §7 1a項「フェーズ1完了の前に各SVGを単体で `<img src=.svg>` でブラウザ自己描画確認する」ゲート追加。
- **commit**：7f08726（層1・層2）／231c493（層3完成）。

### 事例2：L-022 AI画像のFIC配置パス取り違え防止（3層全部）
- **発見**：FICがAI画像を `3861_oji_v4_pilot/images/` ではなく `285A_kioxia_pilot/images/`（前回 designerパイロットのフォルダ）に誤配置していたのを designer が `find -name` で即発見・コピーで復旧。
- **層1**：`docs/ai_image_lessons.md` 新設（AI画像運用ルールの集約先）。「保存先絶対パス明示」「Codex送信プロンプトにも絶対パス明示」「designer フェーズ2の最初に画像存在確認」の3点を規律化。
- **層2**：`review_lessons_log.md` L-022 エントリ。
- **層3**：`.claude/agents/designer.md` §7 フェーズ2 §5 に「画像受領後の `ls / find` で実在確認＋取り違え発見時のコピー復旧」ステップ追加。
- **commit**：43d1a15。

### 事例3：L-023 画像配置原則（章末→章冒頭H2直下/H3直下）（3層全部）
- **発見**：3861パイロットで designer が画像5点を全て章末に配置。FIC実機確認で「画像があってそれを説明する文章が後に来る方が画像を存分に活用できる」と指摘。テンプレ `wordpress/templates/company_analysis_template.html` のHTMLコメントが「H3直下・章冒頭・ドライバー①直前」を想定していたが designer はテンプレ準拠でなかった。
- **層1**：`docs/ai_image_lessons.md` L-023セクション＋ `data_figure_lessons.md` 配置原則追補＋ `non_ai_structure_chart_lessons.md` 配置原則追補（3ファイル）。画像種別ごとの最適配置（AI画像=章冒頭H2直下／データ図=H3直下表の前／冒頭テーブルとの組合せはテーブル→図→H3の順）を規律化。
- **層2**：`review_lessons_log.md` L-023 エントリ。
- **層3**：`.claude/skills/article-design-principles/SKILL.md` §3-1テンプレに「画像配置は章冒頭H2直下 or H3直下を原則とする（L-023）」を明示＋ `.claude/agents/designer.md` §6 ＋ `.claude/agents/writer.md` §4 に画像配置原則を反映。
- **commit**：43d1a15。

---

## 3. 3層型の柔軟運用ルール（L-026相当・全層必須ではない）

3層型は「全層必須」ではなく、**教訓の性質に応じて層を選ぶ**運用が正解。

### 3-1. 教訓の性質別マッピング

| 教訓の性質 | 適用する層 | 理由・例 |
|---|---|---|
| **具体的な作業ルール**（次回同じ作業時に守るべき手順・閾値・形式） | 層1＋層2＋層3（フル3層） | L-021/L-022/L-023。subagent 行動を変える＝層3必須・領域別ガイドへの追補＝層1・教訓履歴記録＝層2 |
| **メタ規律**（設計原則・運用透明性・自己規律） | 層2＋α（CLAUDE.md等のメタ条項） | L-024（設計判断は実装後の実証で更新）／L-025（bypass下でも規律維持）。subagent 行動を直接変えるものではなく、設計判断や運用態度に関わる |
| **データ的一般化**（1事例では普遍か個社固有か判別できない） | 層2のみ（要追加観察） | L-001（FY2026 OUM・FY2025 Kioxia の試算扱い）など。複数事例で普遍性が確認できたら層1へ昇格、subagent行動を変える必要が出たら層3へ昇格 |
| **手戻り・差し戻し対応の運用記録** | 層2のみ | reviewer差し戻しの個別対応履歴。普遍性は薄いが履歴として残す |

### 3-2. 層別の§7ゲート対象判定

- **層1**：`docs/` 配下＝§7対象外＝即時反映可（FIC内容確認のうえ反映）。
- **層2**：`docs/chat_workflows/_analysis/review_lessons_log.md`＝§7対象外＝即時反映可。
- **層3**：
  - `.claude/skills/` 配下＝§7ゲート対象＝**FIC承認必須**。
  - `.claude/agents/` 配下＝§7ゲート対象＝**FIC承認必須**。
  - テンプレ（`wordpress/templates/*.html`）＝§7対象外だがFIC内容確認推奨。

### 3-3. 段階的昇格パス

1事例の教訓は**層2のみ**で記録（curation判定＝要追加観察）→ 複数事例で普遍性が確認できたら**層1へ昇格**（具体ルール化）→ subagent行動を変える必要が出たら**層3へ昇格**（実装ゲート化）、というパスが標準。

---

## 4. C'案基準（即時反映可 vs 段階4送り）

教訓を Skill / guide / template / agent定義 へ反映する判定基準（FIC合意 2026-05-22・C'案）。段階4 curation でも同じ基準で運用する。

### 4-1. 即時反映してよい
- **論理的に根拠づく原則**（1事例でも論理的に普遍性が明らか）
- **repo＋実物（公開HTML/一次資料）の両ソースで裏が取れた普遍ルール**（1事例でも昇格して安全）

### 4-2. 段階4へ送る
- 複数事例で初めて普遍性が見える**“データ的一般化”**（1事例では普遍か個社固有か判別できないもの）

### 4-3. §7ゲート対象＝`.claude/skills/` / `.claude/agents/` 配下のみ
- 上記配下の編集はFIC承認が必要
- `chapter_design_guide.md`・`wordpress/templates/`・本ログ・`docs/` 配下は§7対象外（FIC内容確認のうえ反映で可）

### 4-4. 判定例

| 事例 | C'案判定 | 反映先 |
|---|---|---|
| L-005（反証＝観測パターン閾値） | 論理的原則 → 即時反映 | chapter_design_guide |
| L-006（予想範囲の明示） | 条件付き普遍ルール → 即時反映 | factual-handling §3＋template |
| L-008（文体基調＝です・ます） | 普遍ルール → 即時反映 | writing-style §5 |
| L-002（IFRSラベル） | 両ソース裏取り済み普遍ルール → 即時反映 | factual-handling §3 |
| L-007（ハイパースケーラCapEx） | データ的・文脈依存 → 新規Skill変更せず（記事内のみ反映） | 段階4で再検討 |
| L-021/L-022/L-023 | 論理的原則＋両ソース裏取り済み → 即時反映（3層フル） | 各事例参照 |
| L-024/L-025（メタ規律） | 論理的原則＋両ソース裏取り済み → 即時反映（層2＋α・CLAUDE.md） | review_lessons_log + CLAUDE.md |

---

## 5. 適用ガイドライン（段階4以降の subagent 展開時）

新subagent（例：researcher_industry／scout／theme_scout／videographer／x_writer）作成時の標準プロトコル「**定義→Phase 0→本作業1本→点検→教訓即時反映**」の最後のフェーズで、本書を参照して以下を判断：

1. **教訓の性質を判定**（具体ルール／メタ規律／データ的一般化／運用記録）→ §3-1マッピング
2. **適用する層を決定**（フル3層／層2＋α／層2のみ）
3. **C'案基準で即時反映可否を判定**（§4）
4. **§7ゲート対象か確認**（§3-2）
5. **層1・層2は commit／層3は FIC承認待ち提案**（3段階フロー：起草→壁打ち→FIC承認）
6. **3層反映完了後、本書に新事例として追記**（参照型を蓄積）

### 段階4以降の他ロール作成時のチェックリスト

新subagent定義v1起草時：
- [ ] 既存の3事例（L-021/L-022/L-023）を参照型として読んだか
- [ ] 当該ロールが扱う領域に既存の層1具体ルール（領域別ガイド）があるか確認
- [ ] 既存ガイドの不足箇所を Phase 0 で発見したら、§3-1マッピングで層配分を判断
- [ ] 普遍性が確実な教訓のみ層3まで反映、判別不能なものは層2のみ（要追加観察）

---

## 6. 関連ドキュメント

- `docs/chat_workflows/_analysis/review_lessons_log.md`：教訓本体（L-001〜現在）
- `docs/chat_workflows/_analysis/stage4_scope_notes.md`：段階4標準プロトコル・残り5ロール展開順序
- `docs/data_figure_lessons.md`：層1具体ルール例（データ図SVG）
- `docs/ai_image_lessons.md`：層1具体ルール例（AI画像）
- `docs/non_ai_structure_chart_lessons.md`：層1具体ルール例（構造図）
- `.claude/skills/article-design-principles/SKILL.md`：層3実装ゲート例（SKILL）
- `.claude/agents/{role}.md`：層3実装ゲート例（subagent定義）
- `CLAUDE.md`：メタ規律の集約先（L-024/L-025等の反映先候補）

---

## 7. L-024/L-025/L-027/L-028 の3層適用整理表（FIC視認用・2026-05-24完成）

L-021〜L-023（3事例）に続き、L-024/L-025/L-027/L-028 の4事例で3層型柔軟運用が定着。**「具体的作業ルール型（フル3層）」と「メタ規律型（層2＋α）」の2類型**が明確化：

| 層 | L-024 | L-025 | L-027 | L-028 |
|---|---|---|---|---|
| **層1 具体ルール** | — | — | `expression-strength-rules` SKILL §10新設 | `docs/independent_driver_lessons.md` 新設 |
| **層2 教訓本体** | ✅ commit 012c9d2 | ✅ commit 012c9d2 | ✅ commit 2dc8bc0 | ✅ commit 2dc8bc0 |
| **層3 実装ゲート** | — | — | writer.md §1／reviewer.md §1／article-quality-checklist B-10 | writer.md §1／reviewer.md §1／article-quality-checklist B-11／researcher_company.md §6 |
| **CLAUDE.md §3** | 原則8（commit 710450d） | 原則9（commit 710450d） | 原則10（commit 710450d） | 原則11（commit 710450d） |
| **適用パターン** | **メタ規律型**（層2＋α） | **メタ規律型**（層2＋α） | **フル3層** | **フル3層** |
| **L-027/L-028の運用関係** | — | — | **セット運用**（記事の表現） | **セット運用**（記事の構造） |

### 7-1. 適用パターンの判別ガイド（§3-1の再掲）

| 教訓の性質 | 適用層 | 例 |
|---|---|---|
| **具体的作業ルール** | フル3層 | L-021/L-022/L-023/L-027/L-028（subagent行動を変える） |
| **メタ規律**（設計原則・運用透明性・自己規律） | 層2＋α（CLAUDE.md等のメタ条項） | L-024/L-025（subagent行動を直接変えない） |
| **データ的一般化**（1事例で普遍判別不能） | 層2のみ（要追加観察） | L-001/L-007 など |

### 7-2. CLAUDE.md §3 4メタ規律の論理順序（FIC事前指摘採用）
**事前確認の徹底（L-024）→ ガード規律の維持（L-025）→ FIC独自価値の表出（L-027）→ FIC独自分析の組み込み（L-028）**

各原則の本文は5〜7行で簡潔・詳細は委譲先（SKILL／agents／docs）に集約。

## 8. 権威ある既存資料への依存バイアス対策（L-027/L-028/L-029統合セクション・2026-05-24）

Phase 5移行の核心価値「公開IRより踏み込んだFIC独自分析」を構造的に担保するための**3つのバイアス対策**を統合して扱う。3層型の枠組みで反映済（L-027/L-028はフル3層・L-029は3層FIC承認待ち）。

### 8-1. 3バイアスの体系と発生面

| バイアス | 発生面 | 対策（L番号） |
|---|---|---|
| **記事の表現（FIC意見の控えすぎ）** | writer出力で会計士視点の独自評価を控えめにする | **L-027**（expression-strength-rules §10：FIC意見の積極的表出ルール） |
| **記事の構造（会社のなぞり型ドライバー）** | researcher_companyのドライバー選定が会社開示フレームをなぞる | **L-028**（docs/independent_driver_lessons.md：FIC独自ドライバー選定の規律・4視点） |
| **設計の構造（既存資料の所与扱い）** | Phase 0論点で旧Codex/Make時代の既存資料（template/SOP/workflow）を Phase 5観点で再検証せず所与とする | **L-029**（本セクション：権威ある既存資料への依存バイアス対策・Phase 0標準チェック） |

### 8-2. 3バイアスの共通構造
- **権威性の源泉**：会社開示（L-027/L-028）／既存仕様資料（L-029）
- **再検証の起点**：FIC方針「複数情報源から確度高く推測できる内容は記事/設計に残す」
- **検証ツール**：複数情報源分析（L-027/L-028の4視点）／Phase 5観点再検証（L-029の章構成検証）

### 8-3. Phase 0標準チェック（L-029由来・残り5ロール展開でも適用）
新規 subagent / 新規パイプライン構築時のPhase 0で必須チェック：
- [ ] 既存資料（既存テンプレ／既存workflow SOP／既存記事構造／migration_spec等）を所与として扱っていないか
- [ ] Phase 5観点（FIC独自分析・L-027/L-028・横串原則の数値レベル一貫）で各既存資料の前提を再検証したか
- [ ] 既存資料の構造（章数・セクション数・出力フォーマット）が Phase 5 移行核心価値と整合するか実証データで判定したか
- [ ] 「動いている」「過去のFIC合意」という権威性に依存せず、Phase 5観点で必要な変更を加える判断を下したか

### 8-4. 反証パターン（実証データ）
- **L-027実証**：王子HD初稿で5論点中2点欠如・3点抑制気味＝FIC意見の控えすぎが構造的に発生
- **L-028実証**：王子HD初稿でT4を「会社の中計4軸」のままドライバー化＝T4a/T4b分解（隠れドライバー）がPhase 5観点で必要と判明
- **L-029実証**：業界版章構成を「既存11章＝確定」と扱った→FIC指摘で論点7.5として再検証→13章拡張（業界全体ロードマップ妥当性検証章追加・リスク章独立化・章9/10統合）が必要と判明

→ 3つの実証例がいずれも「権威依存バイアスがFIC指摘で初めて顕在化」した共通パターン。**FIC指摘なしには気づけない構造的バイアス**であるため、3層型反映で**仕組み化**する。

## 9. 追補ログ
- 2026-05-24 初版（3861_oji_v4_pilot 縦ライン検証で L-021/L-022/L-023 の3事例が定着しFIC評価＝標準型として明文化）。3層型柔軟運用ルール（L-026相当）も収録。今後の事例で追補する。
- 2026-05-24 追補：**L-027（FIC意見の積極的表出ルール）＋L-028（FIC独自ドライバー選定ルール）を3層フル適用候補として記録**。両者はセット運用（記事の表現＝L-027／記事の構造＝L-028）で、Phase 5 移行核心価値「FIC独自分析実証（MS2）」の中核規律。L-027/L-028の3層反映は MS2 着手の最優先タスク（D-1案）。詳細は `review_lessons_log.md` L-027/L-028 エントリと `stage4_scope_notes.md` MS2着手前優先事項を参照。
- 2026-05-24 完成（commit bd5f601/bf7d9d1/710450d）：L-024/L-025/L-027/L-028の3層適用が完了。CLAUDE.md §3 に原則8/9/10/11追加。L-027/L-028はフル3層（expression-strength-rules §10新設＋independent_driver_lessons.md新設＋4ファイル層3反映）。L-024/L-025はメタ規律型（層2＋CLAUDE.md §3）。§7に4事例の3層適用整理表を収録。**MS2 Step 3完了** → Step 4（王子HD改訂版・部分改訂）へ。
- 2026-05-24 追補（L-029）：**§8「権威ある既存資料への依存バイアス対策」セクション新設**。L-027/L-028/L-029を統合扱い（記事の表現＝L-027／記事の構造＝L-028／設計の構造＝L-029）。Phase 0標準チェック4項目を§8-3に明示＝残り5ロール展開時に再発防止の仕組み化。L-029は層1（本セクション）＋層2（review_lessons_log.md L-029）完了・層3はFIC承認待ち（壁打ちフェーズ）。
